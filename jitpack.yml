before_install:
  - sdk install java 25-tem
  - sdk use java 25-tem
install:
  - ./gradlew :spi:publishToMavenLocal :common:publishToMavenLocal :domain:publishToMavenLocal :bukkit:publishToMavenLocal
