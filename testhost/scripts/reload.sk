on load:
	while {-rt::loads} is not 1:
		wait 1 tick
	fire test event "reload"
	reload script "reloadtarget.sk"
	while {-rt::loads} is not 2:
		wait 1 tick
	fire test event "reload"
	disable script "reloadtarget.sk"
	wait 2 ticks
	fire test event "reload"
	mark "driver done"
